{
  "name": "kaiwa-trip-drill",
  "version": "0.10.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "start": "node server.js",
    "start:static": "python3 -m http.server 4173 --bind 0.0.0.0",
    "test": "node --test",
    "test:browser": "node scripts/browser-check.mjs",
    "check": "npm test && npm run test:browser"
  }
}
